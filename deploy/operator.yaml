apiVersion: apps/v1
kind: Deployment
metadata:
  name: nodeinfo-operator
spec:
  replicas: 1
  selector:
    matchLabels:
      name: nodeinfo-operator
  template:
    metadata:
      labels:
        name: nodeinfo-operator
    spec:
      serviceAccountName: nodeinfo-operator
      containers:
        - name: nodeinfo-operator
          # Replace this with the built image name
          image: REPLACE_IMAGE
          command:
          - nodeinfo-operator
          imagePullPolicy: Always
          env:
            - name: WATCH_NAMESPACE
              valueFrom:
                fieldRef:
                  fieldPath: metadata.namespace
            - name: POD_NAME
              valueFrom:
                fieldRef:
                  fieldPath: metadata.name
            - name: OPERATOR_NAME
              value: "nodeinfo-operator"
